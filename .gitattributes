# 统一换行符：仓库内一律 LF，避免 Windows 检出 CRLF 后
# shell 脚本（build_apk.sh、gradlew）在 CI 上出现 \r 执行错误
* text=auto eol=lf

*.sh    text eol=lf
gradlew text eol=lf
*.bat   text eol=crlf

*.png   binary
*.jpg   binary
*.jpeg  binary
*.webp  binary
*.apk   binary
*.jar   binary
*.jks   binary
*.keystore binary
