# ══════════════════════════════════════════════════════════════
#  Git 忽略规则
# ══════════════════════════════════════════════════════════════

# ── 构建产物 ──
*.iml
.gradle/
build/
app/build/
captures/
.externalNativeBuild/
.cxx/
release/

# ── 本地配置（含机器相关路径，绝不能提交）──
local.properties

# ── 签名文件 ──
#
# ⚠️ 这一条非常重要：keystore 与密码一旦进仓库，
#    任何人都能签发以你的名义发布的 APK。
#    CI 通过 GitHub secrets 还原，本地开发通过 local.properties 提供。
*.jks
*.keystore
keystore.properties

# ── IDE ──
.idea/
*.hprof
.kotlin/

# ── 系统 ──
.DS_Store
Thumbs.db
*.swp

# ── 日志与临时文件 ──
*.log
*.tmp
