<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 网络：可选，用于把打卡结果推送到通知渠道 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- 定时：精确闹钟（打卡对时间敏感，必须精确） -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission
        android:name="android.permission.USE_EXACT_ALARM"
        tools:ignore="ExactAlarm" />

    <!-- 唤醒：闹钟响时需要把设备从睡眠中唤醒 -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- 前台服务：打卡过程需保持运行不被杀 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission
        android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE"
        tools:ignore="ForegroundServicePermission" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- 电池优化白名单引导 -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!--
      Shizuku 授权声明。
      这是第三方应用使用 Shizuku 能力的**必需声明** —— 没有它，
      Shizuku.checkSelfPermission() 永远返回拒绝，且不会有任何提示。
      注意这不会让应用获得权限，用户仍需在 Shizuku 里手动授权。
    -->
    <uses-permission android:name="moe.shizuku.manager.permission.API_V23" />

    <!--
      读取桌面快捷方式。
      飞书支持把工作台应用（含打卡）添加到桌面，点图标即直达该应用。
      这是我们希望利用的入口之一，但查询快捷方式需要这个权限。
    -->
    <uses-permission
        android:name="com.android.launcher.permission.INSTALL_SHORTCUT"
        tools:ignore="ProtectedPermissions" />

    <!--
      查询飞书包名。
      Android 11+ 的包可见性限制会导致 getLaunchIntentForPackage 返回 null，
      必须显式声明要交互的包。
    -->
    <queries>
        <package android:name="com.ss.android.lark" />
        <package android:name="com.larksuite.suite" />
        <!-- Shizuku：需要查询它是否安装 -->
        <package android:name="moe.shizuku.privileged.api" />
        <package android:name="moe.shizuku.privileged.api.dev" />

        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>

        <!--
          深链跳转需要系统能解析出飞书。
          不声明的话 resolveActivity 会返回 null，导致我们误判为
          「没有应用能打开这个链接」而放弃跳转。
        -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="https" android:host="applink.feishu.cn" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="https" android:host="applink.larksuite.com" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.DEFAULT" />
            <data android:scheme="feishu" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.DEFAULT" />
            <data android:scheme="lark" />
        </intent>
    </queries>

    <application
        android:name=".FeishuCheckInApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.FeishuCheckIn"
        tools:targetApi="34">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.FeishuCheckIn.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          无障碍服务：本项目唯一的自动化执行通道。
          通过读取控件树定位打卡按钮，再以 dispatchGesture / performAction 点击。
          不使用截屏方案：飞书对关键页面设置了 FLAG_SECURE，截图会得到黑屏。
        -->
        <service
            android:name=".accessibility.CheckInAccessibilityService"
            android:exported="true"
            android:label="@string/accessibility_service_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- 闹钟触发入口 -->
        <receiver
            android:name=".checkin.receiver.CheckInAlarmReceiver"
            android:exported="false" />

        <!-- 开机 / 应用更新 / 时区时间变更后重建闹钟 -->
        <receiver
            android:name=".checkin.receiver.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.TIME_SET" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
                <action android:name="android.app.action.SCHEDULE_EXACT_ALARM_PERMISSION_STATE_CHANGED" />
            </intent-filter>
        </receiver>

        <!-- 打卡执行的前台服务 -->
        <service
            android:name=".checkin.service.CheckInService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="scheduled_checkin_automation" />
        </service>

        <!-- 导出日志用 -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
